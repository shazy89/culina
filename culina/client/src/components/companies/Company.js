import React from "react";
import PropTypes from "prop-types";

const Company = (props) => {
  return (
    <div>
      <h1>I'm the Path</h1>
    </div>
  );
};

Company.propTypes = {};

export default Company;
